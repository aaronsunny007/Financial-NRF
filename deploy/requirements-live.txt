# Only needed if NRF_ENABLE_LIVE=1 (running the real pipeline).
# The results explorer does NOT need any of these.
gradio>=4.44.0
pandas>=2.0.0
torch>=2.1.0
transformers>=4.44.0
sentence-transformers>=3.0.0
numpy>=1.24.0
